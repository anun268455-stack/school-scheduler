services:
  - type: web
    name: school-scheduler-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python mock_api.py
    envVars:
      - key: PORT
        value: 10000
    plan: free
