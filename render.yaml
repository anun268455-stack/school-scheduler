services:
  - type: web
    name: school-scheduler-api
    runtime: python
    buildCommand: pip3 install -r requirements.txt
    startCommand: python3 mock_api.py
    envVars:
      - key: PORT
        value: 10000
    plan: free
